[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Exclude UI tests requiring playwright (run separately if needed)
addopts = --ignore=tests/test_customization_ui.py
